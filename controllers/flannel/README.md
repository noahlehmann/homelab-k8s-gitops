# Flannel CNI

See docs here: [https://github.com/flannel-io/flannel](https://github.com/flannel-io/flannel)

```bash
flux reconcile --kubeconfig ../kubeconfig kustomization --with-source controllers -n flux-system
```
