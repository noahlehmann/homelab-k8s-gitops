apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audio-books
spec:
  storageClassName: nfs-named
  volumeName: audio-books
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 2Ti