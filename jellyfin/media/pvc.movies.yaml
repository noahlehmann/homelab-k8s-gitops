apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movies
spec:
  storageClassName: nfs-named
  volumeName: movies
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 2Ti