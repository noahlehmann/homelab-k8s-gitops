apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shows
spec:
  storageClassName: nfs-media
  volumeName: shows
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti