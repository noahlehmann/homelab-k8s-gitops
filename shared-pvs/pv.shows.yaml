apiVersion: v1
kind: PersistentVolume
metadata:
  name: shows
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 2Ti
  csi:
    driver: nfs.csi.k8s.io
    volumeAttributes:
      server: 10.0.0.21
      share: /mnt/data/media
      subdir: shows
    volumeHandle: 10.0.0.21#mnt/data/media#shows##
  mountOptions:
    - nfsvers=4.1
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-media
  volumeMode: Filesystem
