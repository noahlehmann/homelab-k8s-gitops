apiVersion: v1
kind: Service
metadata:
  name: test
  annotations:
    metallb.universe.tf/address-pool: "dmz-pool"
    metallb.universe.tf/loadBalancerIPs: "10.0.0.58"
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
